import {studioTheme, ThemeColorMuted} from '@sanity/ui'
// import legacyTheme from 'sanity:css-custom-properties'

const defaults = studioTheme.color.light.default.muted

export const muted: ThemeColorMuted = {
  ...defaults,
  // @todo
}
